import React from "react";
import logo from "../logo.png";

const Logo = () => {
  return (
    <div className="logo-img">
      <img src={logo} alt="logo-img"></img>
    </div>
  );
};

export default Logo;
