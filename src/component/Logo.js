/** @jsx jsx */
import { css, jsx } from "@emotion/core";

const Logo = props => (
  <div
    css={css`
      display: inline-block;
      height: 1.5rem;
      margin-left: 2rem;
    `}
  >
    LOGO
  </div>
);

export default Logo;
